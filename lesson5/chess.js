class Chess {
  constructor(code, initialPosition, picture) {
      this.code = code;
      this.initialPosition = initialPosition;
      this.picture = picture;
  }

  Move() {
      //todo Реализовать метод перемещения фигуры
  }
}

export default Chess;